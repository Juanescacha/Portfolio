---
import { profile } from "@/data"
import { marked } from "@/utils/marked.ts"

const footer = marked.parse(profile.footer)
---

<footer
	set:html={footer}
	class="footer-markdown max-w-md text-sm text-slate-500 mb-[calc(20vh-48px)] lg:mb-[calc(20vh-96px)] z-50"
/>
